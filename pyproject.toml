[project]
name = "journal-sentiment"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ruff",
    "lefthook",
    "python-dotenv",
    "pandas",
    "matplotlib",
    "pytest",
    "asari>=0.2.0",
]
